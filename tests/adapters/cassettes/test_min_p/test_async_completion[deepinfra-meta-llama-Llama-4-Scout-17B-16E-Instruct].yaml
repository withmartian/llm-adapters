interactions:
- request:
    body: '{"model":"meta-llama/Llama-4-Scout-17B-16E-Instruct","prompt":"Hi"}'
    headers:
      accept:
      - application/json
      accept-encoding:
      - gzip, deflate, br
      connection:
      - keep-alive
      content-length:
      - '67'
      content-type:
      - application/json
      host:
      - api.deepinfra.com
      user-agent:
      - AsyncOpenAI/Python 1.76.0
      x-stainless-arch:
      - arm64
      x-stainless-async:
      - async:asyncio
      x-stainless-lang:
      - python
      x-stainless-os:
      - MacOS
      x-stainless-package-version:
      - 1.76.0
      x-stainless-read-timeout:
      - '600.0'
      x-stainless-retry-count:
      - '0'
      x-stainless-runtime:
      - CPython
      x-stainless-runtime-version:
      - 3.13.1
    method: POST
    uri: https://api.deepinfra.com/v1/openai/completions
  response:
    body:
      string: '{"id":"cmpl-R2pSaNlFZJd1SBYReF5M9k4l","object":"text_completion","created":1746561143,"model":"meta-llama/Llama-4-Scout-17B-16E-Instruct","choices":[{"index":0,"text":":
        \n1. The problem statement is: \nGiven a string s and an integer k, find the
        length of the longest substring that contains at most k distinct characters.\n\n2.
        My approach is to use a sliding window approach with the help of a dictionary
        to store the frequency of characters in the current window.\n\nHere is my
        solution:\n\n```python\ndef length_of_longest_substring_k_distinct(s: str,
        k: int) -> int:\n    if k == 0 or not s:\n        return 0\n\n    char_freq
        = {}\n    left = 0\n    max_length = 0\n\n    for right in range(len(s)):\n        char_freq[s[right]]
        = char_freq.get(s[right], 0) + 1\n\n        while len(char_freq) > k:\n            char_freq[s[left]]
        -= 1\n            if char_freq[s[left]] == 0:\n                del char_freq[s[left]]\n            left
        += 1\n\n        max_length = max(max_length, right - left + 1)\n\n    return
        max_length\n```\n\n3. However, I want to test this function with some test
        cases to ensure it works correctly.\n\nHere are some test cases:\n\n```python\nimport
        unittest\n\nclass TestLengthOfLongestSubstringKDistinct(unittest.TestCase):\n\n    def
        test_empty_string(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"\",
        2), 0)\n\n    def test_k_zero(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        0), 0)\n\n    def test_k_one(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abcba\",
        1), 3)\n\n    def test_k_greater_than_total_chars(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        3), 3)\n\n    def test_k_equal_to_total_chars(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        2), 2)\n\n    def test_long_string(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"eceba\",
        2), 3)\n\nif __name__ == ''__main__'':\n    unittest.main()\n```\n\n4. The
        solution seems correct and the test cases cover various scenarios. \n\nHowever,
        I want to get a second opinion on this.\n\nI''d appreciate it if someone could:\n\n*
        Review the solution for correctness and efficiency.\n* Provide suggestions
        for improvement if any.\n\n**Solution Review**\n\n## Step 1: Correctness Review\nThe
        provided solution uses a sliding window approach with a dictionary to store
        character frequencies. This approach is suitable for the problem.\n\n## Step
        2: Efficiency Review\nThe time complexity of the solution is O(n), where n
        is the length of the string. This is because each character in the string
        is visited at most twice (once by the right pointer and once by the left pointer).\n\n##
        Step 3: Code Review\nThe code is well-structured and readable. The variable
        names are clear, and the comments are not needed as the code is self-explanatory.\n\n##
        Step 4: Suggestions for Improvement\n- The function name `length_of_longest_substring_k_distinct`
        is clear and descriptive. However, consider adding a docstring to describe
        what the function does, its parameters, and its return value.\n\n- Consider
        adding error handling for invalid inputs, such as negative values of k.\n\n-
        The test cases cover various scenarios, but consider adding more test cases
        for edge cases.\n\n## Step 5: Code Refactoring\nHere is a refactored version
        of the code with a docstring and error handling:\n\n```python\ndef length_of_longest_substring_k_distinct(s:
        str, k: int) -> int:\n    \"\"\"\n    Given a string s and an integer k, find
        the length of the longest substring \n    that contains at most k distinct
        characters.\n\n    Args:\n        s (str): The input string.\n        k (int):
        The maximum number of distinct characters.\n\n    Returns:\n        int: The
        length of the longest substring with at most k distinct characters.\n\n    Raises:\n        ValueError:
        If k is negative.\n    \"\"\"\n    if k < 0:\n        raise ValueError(\"k
        cannot be negative\")\n\n    if k == 0 or not s:\n        return 0\n\n    char_freq
        = {}\n    left = 0\n    max_length = 0\n\n    for right in range(len(s)):\n        char_freq[s[right]]
        = char_freq.get(s[right], 0) + 1\n\n        while len(char_freq) > k:\n            char_freq[s[left]]
        -= 1\n            if char_freq[s[left]] == 0:\n                del char_freq[s[left]]\n            left
        += 1\n\n        max_length = max(max_length, right - left + 1)\n\n    return
        max_length\n```\n\n## Step 6: Test Case Refactoring\nThe test cases can be
        refactored to include more edge cases:\n\n```python\nimport unittest\n\nclass
        TestLengthOfLongestSubstringKDistinct(unittest.TestCase):\n\n    def test_empty_string(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"\",
        2), 0)\n\n    def test_k_zero(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        0), 0)\n\n    def test_k_negative(self):\n        with self.assertRaises(ValueError):\n            length_of_longest_substring_k_distinct(\"abc\",
        -1)\n\n    def test_k_one(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abcba\",
        1), 3)\n\n    def test_k_greater_than_total_chars(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        3), 3)\n\n    def test_k_equal_to_total_chars(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"abc\",
        2), 2)\n\n    def test_long_string(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"eceba\",
        2), 3)\n\n    def test_single_char_string(self):\n        self.assertEqual(length_of_longest_substring_k_distinct(\"a\",
        1), 1)\n\nif __name__ == ''__main__'':\n    unittest.main()\n```","logprobs":null,"finish_reason":"stop"}],"usage":{"prompt_tokens":2,"total_tokens":1298,"completion_tokens":1296,"estimated_cost":0.00038896}}'
    headers:
      Connection:
      - keep-alive
      Content-Length:
      - '5848'
      Content-Type:
      - application/json
      Date:
      - Tue, 06 May 2025 19:52:23 GMT
      server:
      - uvicorn
      x-robots-tag:
      - noindex
    status:
      code: 200
      message: OK
version: 1
